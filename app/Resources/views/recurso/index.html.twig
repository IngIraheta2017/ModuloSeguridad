{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recursos list</h1>

    <table>
        <thead>
            <tr>
                <th>Nombrerecurso</th>
                <th>Descripcionrecurso</th>
                <th>Idrecurso</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for recurso in recursos %}
            <tr>
                <td><a href="{{ path('recurso_show', { 'idRecurso': recurso.idRecurso }) }}">{{ recurso.nombreRecurso }}</a></td>
                <td>{{ recurso.descripcionRecurso }}</td>
                <td>{{ recurso.idRecurso }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('recurso_show', { 'idRecurso': recurso.idRecurso }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('recurso_edit', { 'idRecurso': recurso.idRecurso }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recurso_new') }}">Create a new recurso</a>
        </li>
    </ul>
{% endblock %}
