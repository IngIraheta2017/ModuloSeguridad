{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recurso</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombrerecurso</th>
                <td>{{ recurso.nombreRecurso }}</td>
            </tr>
            <tr>
                <th>Descripcionrecurso</th>
                <td>{{ recurso.descripcionRecurso }}</td>
            </tr>
            <tr>
                <th>Idrecurso</th>
                <td>{{ recurso.idRecurso }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recurso_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('recurso_edit', { 'idRecurso': recurso.idRecurso }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
