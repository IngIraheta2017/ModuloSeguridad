{% extends 'base.html.twig' %}

{% block body %}
    <h1>Asignacion</h1>

    <table>
        <tbody>
            <tr>
                <th>Idasignacion</th>
                <td>{{ asignacion.idAsignacion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('asignacion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('asignacion_edit', { 'idAsignacion': asignacion.idAsignacion }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
